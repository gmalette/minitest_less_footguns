require "spec_helper"

describe MinitestLessFootguns do
  it "has a version number" do
    expect(MinitestLessFootguns::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
